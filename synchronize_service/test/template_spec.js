const chai = require('chai');
//chai.config.showDiff = false;
const expect = chai.expect;

describe("Synchronize Empty template test", () => {
    let obj_undefine;

    before(done => {
        done();
    });

    after(done => {
        done();
    });

    it("Empty test", done=> {
        done();
    });
});
