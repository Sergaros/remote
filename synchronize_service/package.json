{
  "name": "remote_synchronize_service",
  "version": "1.0.0",
  "description": "Service for synchronization",
  "main": "server.js",
  "scripts": {
    "test": "cross-env NODE_ENV='test' NODE_PATH='.;libs/.' mocha --harmony -u bdd -R spec -t 500 --recursive --watch",
    "watch": "cross-env NODE_PATH='.;libs/.' mocha -u bdd -R spec -t 500 --recursive --watch",
    "start": "NODE_ENV='production' NODE_PATH='.:libs/.' node --harmony server.js"
  },
  "author": "QUBYX",
  "license": "ISC",
  "dependencies": {
    "busboy": "^0.2.14",
    "config": "^1.25.1",
    "moment": "^2.18.1",
    "mongoose": "^4.9.1",
    "winston": "^2.3.1"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "gulp": "^3.9.1",
    "gulp-env": "^0.4.0",
    "gulp-mocha": "^4.1.0",
    "mocha": "^3.2.0",
    "request": "^2.74.0"
  }
}
